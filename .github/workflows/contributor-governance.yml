name: Contributor Governance

on:
  issues:
    types: [opened, labeled, unlabeled]
  pull_request:
    types: [opened, reopened]
  pull_request_target:
    types: [closed]

jobs:
  governance:
    uses: Kuadrant/.github/.github/workflows/contributor-governance.yml@feat/contributor-governance
    secrets: inherit
